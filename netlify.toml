[build]
  functions = "netlify/functions"
  command = "npm install --prefix backend && npm run build --prefix backend"

[dev]
  command = "npm start"
  port = 3000

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/serverless_backend"
  status = 200
